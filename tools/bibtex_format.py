"""
Automatically format a .bib file so that 
1) Authors are all separated by 'and'
2) Title has an extra pair of curly braces to retain formatting
3) Journals are replaced by a preamble definition for same formatting
4) Same formatting of all entries
"""
import sys


def parse_preamble(fid="premable.bib"):
    """
    Turn the .bib preamble into a dictionary
    """
    pass


def bracket_title(


